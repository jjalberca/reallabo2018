resultado1=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado1=[resultado1 v(:,2)];
end

resultado2=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado2=[resultado2 v(:,2)];
end

resultado3=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado3=[resultado3 v(:,2)];
end

resultado4=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado4=[resultado4 v(:,2)];
end

resultado5=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado5=[resultado5 v(:,2)];
end

resultado6=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado6=[resultado6 v(:,2)];
end

resultado7=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado7=[resultado7 v(:,2)];
end

resultado8=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado8=[resultado8 v(:,2)];
end

resultado9=[];

for i=1:10
    name=sprintf('1V %d',i);
    disp(name);
    v = load(name);
    resultado9=[resultado9 v(:,2)];
end